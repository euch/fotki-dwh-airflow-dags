It's submodule of https://github.com/euch/fotki-dwh 

Run Airflow:
```
helm upgrade --install airflow apache-airflow/airflow --namespace airflow --create-namespace --set postgresql.image.tag="latest"
```

Build custom image with dags and dependencies
```
docker build  --pull -t euch/fdwh-airflow:3.1.5-python3.12-001 .

helm upgrade --install airflow apache-airflow/airflow --namespace airflow-fdwh --create-namespace --set postgresql.image.tag="latest" --set images.airflow.repository=euch/fdwh-airflow --set images.airflow.tag=3.1.5-python3.12-002

kubectl port-forward svc/airflow-api-server 8080:8080 --namespace airflow-fdwh

```

Delete namespace and check PVCs
```
 helm delete airflow --namespace airflow

 kubectl get pvc -n airflow
```